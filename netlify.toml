# Netlify 設定ファイル
# このブランチ（claude/lp-creation-iy9veb）では不用品回収LP（lp/）だけを公開します
# ※ main ブランチの netlify.toml は publish = "." のままなので、奨学金ページ側には影響しません

[build]
  publish = "lp"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
